# 传统 AI 搜索效率

基线为 `71d02fa06c7bb4ac80cbe019e83a2e772373965b`。客户端仍请求 5000 毫秒，
Action 4、ChoiceView 2、Snapshot 3、Protocol 6、卡牌内容及作者策略权重不变。
本文区分保持决策的计算优化与会改变决策的渐进搜索，验收结果分别记录。

## 架构与边界

| 层 | 职责与本轮调整 |
| --- | --- |
| Godot 适配器 | 保留异步请求、取消代次、控制器生命周期与 5000 ms 请求预算。 |
| `ChallengeController` | 维护公开信息记忆、计划前提与防循环；创建请求上下文，先取得合法备用动作。 |
| `DecisionSearchContext` | 共享采样根、初始候选终态、精确求值、已完成的场景结果和工作量计数。 |
| `turn_beam_v2` | 保留独立入口；也向 v3 提供有界初始方案和实际模拟终态。 |
| `strategic_intent_v3` | 合并初始方案与战略意图，分批继续同一棵束搜索树，比较反击及下回合接续。 |
| `ChallengeSearchProvider` | 统一排序、完整评分、选牌策略与模拟适配；计算过程不改变卡牌规则。 |
| `RulesSession` | 继续负责合法性及状态推进，沿用搜索分叉和写时复制。 |

静态知识保留在控制器配置的生命周期中。阶段二进一步将卡牌效果和支援者类型解析移入
不可变索引，避免每个请求重新预热整张卡表；未知卡号返回空特征，不在工作线程中懒插入缓存。

## 精确计算优化

- 配置时构建进化关系及名称索引；一次攻击手分析复用相同费用的能量安排。
- 只遍历可用能量，跳过同名能量的重复评分；已经满足费用时保留原结果并提前返回。
- 请求级 `DecisionSearchContext` 保留 COW 状态，复用区域计数、攻击手分析、
  局面评分、状态指纹及继续执行前提。状态修改会分离存储，缓存持有原值，避免地址复用误命中。
- 评估键区分状态、规则会话依据、玩家、RNG 和评分策略。区域计数仅依赖两组精确区域数组。
  请求结束即释放动态缓存；控制器只跨请求共享不可变知识和既有的公开信息计划记忆。

冻结的第一阶段构建对 302 条公开请求预热一次、正式测量三轮。双方分别维护每个座位的控制器，
完整回放请求以保持奖赏知识及计划记忆，并交替执行先后顺序。以下是单线程、固定工作模式的数据：

| 指标 | 当前基线 | 第一阶段 | 变化 |
| --- | ---: | ---: | ---: |
| 搜索 P50 | 1697.42 ms | 1070.93 ms | −36.9% |
| 搜索 P95 | 5106.81 ms | 2965.36 ms | −41.9% |
| 所有决策 P95 | 3869.76 ms | 2337.88 ms | −39.6% |
| 缓存命中 P95 | 372.99 ms | 269.71 ms | −27.7% |

906 次正式核对没有动作、选牌、计划序列或评分差异。第一阶段的 30% 提速目标通过。
原始证据在 `build/traditional-ai-efficiency/stage1-timing`。该结果不代表渐进搜索的棋力或游戏线程配置的延迟。

## 渐进搜索

`DecisionBudget` 统一覆盖准备、候选、选牌、反击、接续和验证。初始策略阶段最多使用
总预算的 20%，末尾预留 5%、最多 250 毫秒供验证和返回；未使用的初始时间留给后续搜索。
预算为零时执行固定规模搜索，适合确定性回归；显式取消与预算到期分别处理。

初始策略以有界 v2 搜索提供可用动作和模拟终态，按平台允许的线程数并行采样。
v3 接收全部已完成的初始候选，直接接续这些状态，按 `node_budget`
大小分批展开，并保留尚未完成的层、父节点和动作索引。根动作分别保留准备路线；
常规深度沿用原有范围，准备接近完成时可延伸两步，总深度最多 10。
根动作保底不会占满束宽，其余名额继续按方案评分分配给准备路线。

动作先进行低成本排序，再详细评分前 16 项及额外受保护的终结、资源和换位路线。
反击和接续优先比较当前方案与前三个竞争方案。双方使用相同样本编号及完整评估阶段，
先处理一个样本，达不到风险下限即淘汰；其余样本可并行计算，按固定顺序归并。
完成的序列回放和交换结果在请求内复用。初始搜索与后续比较使用同一组隐藏信息采样种子；
反击和接续阶段也保护攻击及结束回合，避免准备动作挤掉所有能到达回合边界的路线。

截止后不会切换评分公式；未完成评分带显式状态，不参与完整比较或进入缓存。
每次改进均先进行权威规则验证，再替换当前可提交方案。另一条路线仅仅先执行了
END_TURN，不能作为优于尚未完成的准备路线的证据。
若截止跨越多个采样任务，所有保留样本退到共同完成的评分阶段；尚未完成根动作覆盖的样本
不参与投票。验证器持有动作副本，规则推进后不再引用被失效的合法动作缓存。

独立 `turn_beam_v2` 保留。内部评估可设置 `internal_anytime_search=false` 关闭新的初始预算和渐进调度，
或设置 `internal_search_memoization=false` 关闭精确求值备忘。`internal_full_diagnostics=true`
可要求唯一动作的快速出口也计算战略事实，供资源模型诊断使用。

## 评测口径

Arena 新增 `--time-budget-ms` 与 `--search-worker-mode single|gameplay`，双方必须相同。
时间预算被直接传入控制器，watchdog 只处理基础设施超时。固定工作模式保留确定性标记，
时间模式标记为墙钟预算。游戏线程模式最多并发四局；冒烟模式仍强制每次搜索一个线程。

`native_performance_counters` 增补实际规则动作、选择结算、详细评分数量、各类缓存命中、
完整方案比较、候选批次、三个阶段的耗时及被中断的评估数量。
`budget_last_stop_reason` 区分初始阶段上限、搜索上限、硬截止及显式取消；
`reused_initial_plans`、排序缓存命中和完整比较数量用于核实节省的工作去了哪里。
`node_budget` 是候选构建规模，不能作为所有搜索工作的总成本；性能报告同时保留实际工作量和时间。

阶段二的对局、发布库和最终验收正在进行。开发试验与正式验收使用独立目录，
失败或提前停止的开发批次不纳入验收统计。

开发版本 `progressive_02` 的 160 局结果为 83 胜、77 负，得分率 51.875%，
配对区间为 45.0%–59.375%，结构错误、截断及持续超时均为零。
该版本未证明棋力提升；后续版本补全了初始候选共享、样本复用、额外束宽和反击边界覆盖，
必须重新冻结并独立评估，不能将这批结果并入后续验收。

最终冻结候选的实现哈希为 `acb6ac25bcf1d5d097cf9a741cff96fd0882fd7aee006c406d55dbfd0280c32e`，
构建输入哈希为 `caadbf11cb096e5ce62e817aabf777c9741322acb1b34e994a4a66c49c351c87`。
继续执行缓存额外区分战略评分、牌库检查和初始搜索策略；改变这些策略后会重新决策。

当前已完成的验证：

- 109 个原生战术场景、规则核心回归，以及可控时钟、写时复制和缓存隔离检查通过。
- `test_challenge*.py` 全量 88 项通过，覆盖策略切换失效、中断后恢复、能量共享和关键组合选牌。
- AddressSanitizer 重放三处生命周期场景通过，包含两处历史故障及多步胜利方案验证，标准错误为空。
- 同一候选对 313 条请求进行固定工作回放，单线程和三线程的动作、选牌、序列与评分完全一致。
- Windows x86_64、Android arm64 的 debug/release 四份库完成隔离构建；PE/ELF 机器类型核验通过。
  隔离 Godot 项目的原生协议和十套牌完整对局回归通过。`OutputProjectRoot` 参数支持先构建到隔离项目再更新产品库。

耗时轨迹由原有 302 条请求和 11 条钢牌组后期请求组成，覆盖到第 32 回合、13 张手牌；
原有轨迹还包含五条可选概率攻击的请求。后期部分仅由冻结基线生成，与候选的结果无关。
正式验收已声明三个独立种子，165 个牌组配对种子与历史 310 个配对种子无交集。
正式对局正在执行，不能用上述正确性检查推定棋力已经通过。

首批 400 局及 400 局固定对手对照已完成：候选 208 胜、192 负，得分率 52.0%，
顺序校正后的区间为 47.25%–56.75%。`psychic`、`lightning` 的固定对手差值为
−12.5、−7.5 个百分点，未满足牌组退化门槛。首批状态为 `inconclusive`，
构建和策略继续冻结，按原声明执行第二批。运行库尚未发布到产品目录。

完整耗时测量已结束，均为 5000 ms 预算、单局串行回放，预热一轮后正式测量三轮，
每个构建、每种线程配置各 939 次正式请求：

| 指标 | 基线单线程 | 候选单线程 | 基线三线程 | 候选三线程 |
| --- | ---: | ---: | ---: | ---: |
| 搜索响应 P50 | 1899.57 ms | 753.73 ms | 985.15 ms | 514.57 ms |
| 搜索响应 P95 | 5005.16 ms | 1825.50 ms | 2568.27 ms | 1332.08 ms |
| 全部请求 P50 | 36.04 ms | 209.88 ms | 34.27 ms | 162.33 ms |
| 全部请求 P95 | 4412.00 ms | 1577.20 ms | 1935.04 ms | 1077.20 ms |
| 全部请求最大值 | 5017.00 ms | 3037.29 ms | 3318.08 ms | 2037.96 ms |

搜索响应按各自返回类型统计，两种策略的搜索响应数量不同；全部请求指标使用相同完整输入集。
三线程的完整请求 P95 下降约 44.3%，但中位耗时上升约 128 ms：
新的调度会在部分原先直接出牌的局面继续比较路线。桌面 P95 不超过五秒的目标通过，
这个结果不能代替棋力验收。三线程首轮有 58 个根动作响应变化、零个选牌响应变化，
共完成 85 次共享场景比较。

四个预选代表局面的消融保持相同构建、预算与线程设置：关闭与开启精确状态求值缓存时，
固定工作模式的动作、序列和评分完全一致，平均耗时为 1335.82/1327.12 ms，
P95 为 2164.27/2155.59 ms。这组小样本没有显示该缓存层对 P95 的明显收益。
该开关不关闭静态知识、采样根及已完成场景的共享。
关闭与开启渐进调度时，三线程五秒预算下平均耗时为 1207.77/1014.96 ms，
P95 为 1870.30/1700.60 ms；此比较会改变方案和评分，不能视为保持决策的纯计算优化。

Android 当前没有连接真机。交叉编译和产物检查不提供手机实际延迟的结论。

## 并行开发：同时保留两种评分看好的路线

在 final-04 首批验收完成后，用户授权边监控边继续优化。正式队列保持冻结；
开发位于 `build/traditional-ai-efficiency/development-next`，分支 `codex/traditional-ai-efficiency-next`。
`35cd503` 保存了原冻结版本的实现，`82ca9d3` 增加候选保留方式，`5337dac` 修复其获胜方案筛选。
主工作区的 native、Arena 和研究绑定输入没有随之改变。

现有树中，每个根动作的战略评分最佳路线会保留，但传统局面评分看好的另一种出牌顺序
仍可能被剪掉。新方式沿用两个现有评分公式，在共享树内分别维护它们的最佳准备路线和
完整终态；扩展动作额外保留传统排序的两个不同用途候选。三个竞争名额由两种排序共同
提供，并尽可能覆盖另一个根动作。两种分数只用于各自排序，不相加、不调整作者权重；
最终仍通过相同样本上的反击、接续、原有收益门槛和安全验证后才替换当前方案。

`internal_dual_guidance=false` 可关闭这一变化，用于固定工作兼容和消融比较。
独立 v2 入口与初始搜索规模保持一致。继续执行缓存包含此策略开关，
诊断增加传统路线保留数量、比较数量，以及按需输出的完整候选序列。

guided-01 的控制器回归发现：去掉对当前方案的重复比较时，也跳过了已证明获胜的当前方案，
使它无法进入战略计划验证和缓存。38 项回归中出现 11 个失败，均指向这个筛选问题；
guided-02 保留获胜方案的验证入口，38 项控制器回归全部通过。该开发版本没有进入正式胜率队列。
新增 `test_challenge_agent_controller.py` 对冻结代理运行同一组真实控制器测试，
权威规则由现有绑定提供，AI 决策及取消、继续执行均通过冻结原生代理完成。

开发构建及对照限制为单个逻辑处理器、Idle 优先级，资源约束写入各运行的 `status.json`。
并行期间的时间只用于排错；新的延迟与棋力结论需要单独验证，不能并入 final-04 的结果。

guided-02 的实现哈希为 `2ce1fdf9b0c4e1e4359d8aa3a7e7cc199b561773a248d6f2d1765aaae76813a1`，
构建输入哈希为 `0e954eade9e74ce7de247d90751b4c8d39e41bcfba82db070661ec44a9b7037c`。
对超能／雷的完整公开请求前缀共 47 条执行固定工作对照，关闭新方式时，与 final-04 的
动作、选牌、序列和评分完全一致。开启后只有 1 条请求发生上述语义变化；共额外保留
53 条由传统评分选中的准备路线，24 次竞争比较取自传统评分候选，完整比较数量保持 19 次。

变化案例为原始轨迹索引 99（超能第 7 回合）：final-04 给战斗场附能并攻击，guided-02
保持给 `bench_0` 附能的原策略，与原始 71d02fa 基线一致。这说明候选覆盖能影响稀缺资源的
分配；该样例的因果棋力收益尚未验证，不能用单个决策变化代替对局验收。

证据位于 `build/traditional-ai-efficiency/guided-development/traditional-efficiency-guided-02/`。
新候选在这 47 条请求上的单／三线程固定工作回放也完全一致。
独立 v2 的 313 条完整轨迹兼容回放继续执行；本任务已设置每 30 分钟自动跟进后续验证。
